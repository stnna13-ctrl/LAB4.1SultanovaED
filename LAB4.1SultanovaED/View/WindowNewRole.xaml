<Window x:Class="WpfAppSultanovaED.View.WindowNewRole"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          mc:Ignorable="d"
          Title="Добавление новой должности" Height="200" Width="350"
          Background="White">

    <Grid Margin="10" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0">Название должности:</Label>
        <TextBox Grid.Row="0" Grid.Column="1"
                 Name="TbRole"
                 Text="{Binding NameRole, UpdateSourceTrigger=PropertyChanged}"
                 PreviewTextInput="TbRole_PreviewTextInput"/>

        <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                Name="BtSave" Content="Сохранить" Click="BtSave_Click"
                Background="DarkGray" Foreground="White" Padding="5,2"/>
    </Grid>
</Window>